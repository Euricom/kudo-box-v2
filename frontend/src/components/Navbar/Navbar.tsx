import classes from './NavBar.module.scss';

const Navbar = () => {
    return (
        <nav className={classes.navbar}>
            <h2>Event Room</h2>
        </nav>
    )
}

export default Navbar;